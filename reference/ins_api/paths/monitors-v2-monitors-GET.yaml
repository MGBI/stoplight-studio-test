tags:
- monitors_iapi
summary: Get Monitors
operationId: get_monitors_v2_monitors_get
parameters:
- required: false
  schema:
    title: Exclude
    type: array
    items:
      enum:
      - notifications
      - ''
      type: string
    default:
    - notifications
  name: exclude
  in: query
- required: false
  schema:
    title: Authorization
    type: string
  example: aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee
  name: Authorization
  in: header
  description: Klucz autoryzacji
responses:
  '200':
    description: Returned the list of monitors assigned to the current user.
    content:
      application/json:
        schema:
          title: Response Get Monitors V2 Monitors Get
          type: array
          items:
            $ref: '#/components/schemas/MonitorModel'
  '400':
    description: Bad Request
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/Error'
  '401':
    description: Unauthorized
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/Error'
  '403':
    description: Forbidden
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/Error'
description: Zwraca listę monitorów przypisanych do aktualnego użytkownika.
components:
  schemas:
    $ref: ../../common/schemas.yaml
